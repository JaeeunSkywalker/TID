TIL
1) 반응형 페이지 만들기 위해 패키지 쓸 필요 없고, 안 쓰는 게 좋다.
2) 나만의 보일러 코드를 만들어 놓고 활용하자.
ResponsiveCenter
ResponsiveScrollableCard
ResponsiveTwoColumnLayout
잘 쓰면 된다.
3) Localizations는 프로젝트 만든 초반에 하는 게 좋다.

4) List<Product> products;
화면에 뿌리는 법

For each item:
product = products[index]
ProductScreen(product.id)

Inside ProductScreen:
product = find(productID)
render(product)

> 하지만 이렇게 하면 ID로 계속 lookup해야 하는 문제가 생긴다.
해결법은 > go_router 가지고 해결할 수 있다!
GoRoute(
            path: 'product/:id',
이런 식으로.

4) 못 마친 일이 있으면 밤을 새서라도 해야 한다.

5) 치파스 바닥부터 다시 만들기 시작.

6)처음으로 면접 제안 받은 날.
